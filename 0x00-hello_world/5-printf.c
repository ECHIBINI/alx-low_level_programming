#include <stdio.h>

/**
 * Main prints "with proper grammar, but the outcome is a piece of art, on
 * a new line
 * Return:zero if exited properly
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
